<%= form_for @wiki do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
 
  <%= f.label :body %>
  <%= f.text_area :body %>
 	<%= f.select(:collaborators, options_for_select(@user_options) ) %>
  <%= f.submit "Save" %>
<% end %>